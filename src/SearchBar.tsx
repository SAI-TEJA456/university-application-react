const SearchBar = () => {
    return (
        <form onSubmit={SearchBar}>
            <input type="text" className="search_input" value={"Search for Universities"}/>
        </form>
    )
}

export default SearchBar;